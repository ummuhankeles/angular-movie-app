import { SlideDirectiveDirective } from './slide-directive.directive';

describe('SlideDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
